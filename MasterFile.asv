function MasterFile(filename)

%Generate title string, assumes that CDF name if formatted
temp = size(filename);
titleLen = temp(2);
titleStart = titleLen - 27;
titleEnd = titleLen - 4;
title = filename(titleStart:titleEnd);



data = ProcessCDF(filename);
CDFMillerPlot(data, title, [0,0,1,1]);


end